/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct Vec2 {
  uint64_t x;
  uint64_t y;
} Vec2;

/* Forward declarations */
Vec2 vec_new(uint64_t x __attribute__((unused)), uint64_t y __attribute__((unused)));
Vec2 vec_add(Vec2 a __attribute__((unused)), Vec2 b __attribute__((unused)));
Vec2 vec_scale(Vec2 v __attribute__((unused)), uint64_t s __attribute__((unused)));
uint64_t vec_dot(Vec2 a __attribute__((unused)), Vec2 b __attribute__((unused)));
uint64_t vec_manhattan(Vec2 a __attribute__((unused)), Vec2 b __attribute__((unused)));
uint64_t vec_chebyshev(Vec2 a __attribute__((unused)), Vec2 b __attribute__((unused)));
uint64_t vec_is_axis(Vec2 v __attribute__((unused)));
Vec2 vec_min(Vec2 a __attribute__((unused)), Vec2 b __attribute__((unused)));
Vec2 vec_max(Vec2 a __attribute__((unused)), Vec2 b __attribute__((unused)));
uint64_t add_commutative(Vec2 a __attribute__((unused)), Vec2 b __attribute__((unused)));
uint64_t scale_distrib(Vec2 a __attribute__((unused)), Vec2 b __attribute__((unused)), uint64_t s __attribute__((unused)));
int main();

Vec2 vec_new(uint64_t x __attribute__((unused)), uint64_t y __attribute__((unused))) {
  return (Vec2){ .x = x, .y = y };
}

Vec2 vec_add(Vec2 a __attribute__((unused)), Vec2 b __attribute__((unused))) {
  return (Vec2){ .x = (a.x + b.x), .y = (a.y + b.y) };
}

Vec2 vec_scale(Vec2 v __attribute__((unused)), uint64_t s __attribute__((unused))) {
  return (Vec2){ .x = (v.x * s), .y = (v.y * s) };
}

uint64_t vec_dot(Vec2 a __attribute__((unused)), Vec2 b __attribute__((unused))) {
  return ((a.x * b.x) + (a.y * b.y));
}

uint64_t vec_manhattan(Vec2 a __attribute__((unused)), Vec2 b __attribute__((unused))) {
  uint64_t dx;
  if ((a.x >= b.x)) {
    dx = (a.x - b.x);
  } else {
    dx = (b.x - a.x);
  }
  uint64_t dy;
  if ((a.y >= b.y)) {
    dy = (a.y - b.y);
  } else {
    dy = (b.y - a.y);
  }
  return (dx + dy);
}

uint64_t vec_chebyshev(Vec2 a __attribute__((unused)), Vec2 b __attribute__((unused))) {
  uint64_t dx;
  if ((a.x >= b.x)) {
    dx = (a.x - b.x);
  } else {
    dx = (b.x - a.x);
  }
  uint64_t dy;
  if ((a.y >= b.y)) {
    dy = (a.y - b.y);
  } else {
    dy = (b.y - a.y);
  }
  if ((dx >= dy)) {
    return dx;
  } else {
    return dy;
  }
}

uint64_t vec_is_axis(Vec2 v __attribute__((unused))) {
  if (((v.x == 0) || (v.y == 0))) {
    return 1;
  } else {
    return 0;
  }
}

Vec2 vec_min(Vec2 a __attribute__((unused)), Vec2 b __attribute__((unused))) {
  uint64_t mx;
  if ((a.x <= b.x)) {
    mx = a.x;
  } else {
    mx = b.x;
  }
  uint64_t my;
  if ((a.y <= b.y)) {
    my = a.y;
  } else {
    my = b.y;
  }
  return (Vec2){ .x = mx, .y = my };
}

Vec2 vec_max(Vec2 a __attribute__((unused)), Vec2 b __attribute__((unused))) {
  uint64_t mx;
  if ((a.x >= b.x)) {
    mx = a.x;
  } else {
    mx = b.x;
  }
  uint64_t my;
  if ((a.y >= b.y)) {
    my = a.y;
  } else {
    my = b.y;
  }
  return (Vec2){ .x = mx, .y = my };
}

uint64_t add_commutative(Vec2 a __attribute__((unused)), Vec2 b __attribute__((unused))) {
  Vec2 ab __attribute__((unused)) = vec_add(a, b);
  Vec2 ba __attribute__((unused)) = vec_add(b, a);
  if (((ab.x == ba.x) && (ab.y == ba.y))) {
    return 1;
  } else {
    return 0;
  }
}

uint64_t scale_distrib(Vec2 a __attribute__((unused)), Vec2 b __attribute__((unused)), uint64_t s __attribute__((unused))) {
  Vec2 ab __attribute__((unused)) = vec_add(a, b);
  Vec2 sab __attribute__((unused)) = vec_scale(ab, s);
  Vec2 sa __attribute__((unused)) = vec_scale(a, s);
  Vec2 sb __attribute__((unused)) = vec_scale(b, s);
  uint64_t lhs __attribute__((unused)) = sab.x;
  uint64_t rhs __attribute__((unused)) = (sa.x + sb.x);
  if ((lhs == rhs)) {
    return 1;
  } else {
    return 0;
  }
}

int main() {
  Vec2 a __attribute__((unused)) = vec_new(3, 4);
  Vec2 b __attribute__((unused)) = vec_new(1, 2);
  Vec2 c __attribute__((unused)) = vec_add(a, b);
  uint64_t cx __attribute__((unused)) = c.x;
  uint64_t cy __attribute__((unused)) = c.y;
  Vec2 sc __attribute__((unused)) = vec_scale(a, 2);
  uint64_t scx __attribute__((unused)) = sc.x;
  uint64_t dot __attribute__((unused)) = vec_dot(a, b);
  uint64_t man __attribute__((unused)) = vec_manhattan(a, b);
  uint64_t cheb __attribute__((unused)) = vec_chebyshev(a, b);
  Vec2 mn __attribute__((unused)) = vec_min(a, b);
  Vec2 mx __attribute__((unused)) = vec_max(a, b);
  uint64_t mnx __attribute__((unused)) = mn.x;
  uint64_t mxy __attribute__((unused)) = mx.y;
  uint64_t ax __attribute__((unused)) = vec_is_axis(vec_new(0, 7));
  uint64_t comm __attribute__((unused)) = add_commutative(a, b);
  uint64_t dist __attribute__((unused)) = scale_distrib(a, b, 3);
  return (int)(((((((((((cx + cy) + scx) + dot) + man) + cheb) + mnx) + mxy) + ax) + comm) + dist));

}

