/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct Flow {
  uint64_t sent;
  uint64_t received;
  uint64_t dropped;
} Flow;

/* Forward declarations */
Flow flow_new();
void flow_send(Flow* f __attribute__((unused)), uint64_t n __attribute__((unused)));
void flow_receive(Flow* f __attribute__((unused)), uint64_t n __attribute__((unused)));
void flow_drop(Flow* f __attribute__((unused)), uint64_t n __attribute__((unused)));
uint64_t send_100();
uint64_t send_receive();
uint64_t in_flight();
uint64_t multi_send();
int main();

Flow flow_new() {
  return (Flow){ .sent = 0, .received = 0, .dropped = 0 };
}

void flow_send(Flow* f __attribute__((unused)), uint64_t n __attribute__((unused))) {
  (*f).sent = ((*f).sent + n);
}

void flow_receive(Flow* f __attribute__((unused)), uint64_t n __attribute__((unused))) {
  (*f).received = ((*f).received + n);
}

void flow_drop(Flow* f __attribute__((unused)), uint64_t n __attribute__((unused))) {
  (*f).dropped = ((*f).dropped + n);
}

uint64_t send_100() {
  Flow f __attribute__((unused)) = flow_new();
  flow_send((&f), 100);
  return f.sent;
}

uint64_t send_receive() {
  Flow f __attribute__((unused)) = flow_new();
  flow_send((&f), 100);
  flow_receive((&f), 80);
  return f.received;
}

uint64_t in_flight() {
  Flow f __attribute__((unused)) = flow_new();
  flow_send((&f), 100);
  flow_receive((&f), 70);
  flow_drop((&f), 20);
  return ((f.sent - f.received) - f.dropped);
}

uint64_t multi_send() {
  Flow f __attribute__((unused)) = flow_new();
  flow_send((&f), 30);
  flow_send((&f), 50);
  flow_send((&f), 20);
  return f.sent;
}

int main() {
  return (int)(0);

}

