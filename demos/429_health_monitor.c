/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct HealthMonitor {
  uint64_t consecutive_fails;
  uint64_t total_checks;
  uint64_t total_fails;
  uint64_t passes;
} HealthMonitor;

/* Forward declarations */
HealthMonitor hm_new();
void hm_fail(HealthMonitor* h __attribute__((unused)));
void hm_pass(HealthMonitor* h __attribute__((unused)));
uint64_t three_fails();
uint64_t fail_then_pass();
uint64_t total_checks();
uint64_t total_fails_preserved();
int main();

HealthMonitor hm_new() {
  return (HealthMonitor){ .consecutive_fails = 0ULL, .total_checks = 0ULL, .total_fails = 0ULL, .passes = 0ULL };
}

void hm_fail(HealthMonitor* h __attribute__((unused))) {
  (*h).consecutive_fails = ((*h).consecutive_fails + 1ULL);
  (*h).total_checks = ((*h).total_checks + 1ULL);
  (*h).total_fails = ((*h).total_fails + 1ULL);
}

void hm_pass(HealthMonitor* h __attribute__((unused))) {
  (*h).consecutive_fails = 0ULL;
  (*h).total_checks = ((*h).total_checks + 1ULL);
  (*h).passes = ((*h).passes + 1ULL);
}

uint64_t three_fails() {
  HealthMonitor h __attribute__((unused)) = hm_new();
  hm_fail((&h));
  hm_fail((&h));
  hm_fail((&h));
  return h.consecutive_fails;
}

uint64_t fail_then_pass() {
  HealthMonitor h __attribute__((unused)) = hm_new();
  hm_fail((&h));
  hm_fail((&h));
  hm_pass((&h));
  return h.consecutive_fails;
}

uint64_t total_checks() {
  HealthMonitor h __attribute__((unused)) = hm_new();
  hm_fail((&h));
  hm_pass((&h));
  hm_fail((&h));
  hm_fail((&h));
  hm_pass((&h));
  return h.total_checks;
}

uint64_t total_fails_preserved() {
  HealthMonitor h __attribute__((unused)) = hm_new();
  hm_fail((&h));
  hm_fail((&h));
  hm_fail((&h));
  hm_pass((&h));
  return h.total_fails;
}

int main() {
  return (int)(0ULL);

}

