/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct FreeList {
  uint64_t free;
  uint64_t allocated;
  uint64_t capacity;
} FreeList;

/* Forward declarations */
FreeList fl_new(uint64_t cap __attribute__((unused)));
void fl_alloc(FreeList* f __attribute__((unused)));
void fl_dealloc(FreeList* f __attribute__((unused)));
uint64_t alloc_four();
uint64_t alloc_four_free();
uint64_t alloc3_dealloc2();
uint64_t invariant_holds(uint64_t cap __attribute__((unused)));
int main();

FreeList fl_new(uint64_t cap __attribute__((unused))) {
  return (FreeList){ .free = cap, .allocated = 0ULL, .capacity = cap };
}

void fl_alloc(FreeList* f __attribute__((unused))) {
  (*f).free = ((*f).free - 1ULL);
  (*f).allocated = ((*f).allocated + 1ULL);
}

void fl_dealloc(FreeList* f __attribute__((unused))) {
  (*f).allocated = ((*f).allocated - 1ULL);
  (*f).free = ((*f).free + 1ULL);
}

uint64_t alloc_four() {
  FreeList f __attribute__((unused)) = fl_new(8ULL);
  fl_alloc((&f));
  fl_alloc((&f));
  fl_alloc((&f));
  fl_alloc((&f));
  return f.allocated;
}

uint64_t alloc_four_free() {
  FreeList f __attribute__((unused)) = fl_new(8ULL);
  fl_alloc((&f));
  fl_alloc((&f));
  fl_alloc((&f));
  fl_alloc((&f));
  return f.free;
}

uint64_t alloc3_dealloc2() {
  FreeList f __attribute__((unused)) = fl_new(8ULL);
  fl_alloc((&f));
  fl_alloc((&f));
  fl_alloc((&f));
  fl_dealloc((&f));
  fl_dealloc((&f));
  return f.allocated;
}

uint64_t invariant_holds(uint64_t cap __attribute__((unused))) {
  FreeList f __attribute__((unused)) = fl_new(cap);
  fl_alloc((&f));
  fl_alloc((&f));
  fl_dealloc((&f));
  fl_alloc((&f));
  return (f.free + f.allocated);
}

int main() {
  return (int)(0ULL);

}

