/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct Handle {
  uint64_t state;
  uint64_t pos;
  uint64_t size;
} Handle;

/* Forward declarations */
Handle handle_new();
void handle_open_read(Handle* h __attribute__((unused)), uint64_t size __attribute__((unused)));
void handle_open_write(Handle* h __attribute__((unused)));
void handle_write(Handle* h __attribute__((unused)), uint64_t nbytes __attribute__((unused)));
void handle_close(Handle* h __attribute__((unused)));
uint64_t read_cycle(uint64_t size __attribute__((unused)));
uint64_t write_cycle(uint64_t n __attribute__((unused)));
uint64_t reopen_cycle(uint64_t size __attribute__((unused)));
int main();

Handle handle_new() {
  return (Handle){ .state = 0ULL, .pos = 0ULL, .size = 0ULL };
}

void handle_open_read(Handle* h __attribute__((unused)), uint64_t size __attribute__((unused))) {
  (*h).state = 1ULL;
  (*h).pos = 0ULL;
  (*h).size = size;
}

void handle_open_write(Handle* h __attribute__((unused))) {
  (*h).state = 2ULL;
  (*h).pos = 0ULL;
}

void handle_write(Handle* h __attribute__((unused)), uint64_t nbytes __attribute__((unused))) {
  (*h).pos = ((*h).pos + nbytes);
}

void handle_close(Handle* h __attribute__((unused))) {
  (*h).state = 0ULL;
}

uint64_t read_cycle(uint64_t size __attribute__((unused))) {
  Handle h __attribute__((unused)) = handle_new();
  handle_open_read((&h), size);
  handle_close((&h));
  return h.state;
}

uint64_t write_cycle(uint64_t n __attribute__((unused))) {
  Handle h __attribute__((unused)) = handle_new();
  handle_open_write((&h));
  handle_write((&h), n);
  handle_close((&h));
  return h.state;
}

uint64_t reopen_cycle(uint64_t size __attribute__((unused))) {
  Handle h __attribute__((unused)) = handle_new();
  handle_open_write((&h));
  handle_close((&h));
  handle_open_read((&h), size);
  handle_close((&h));
  return h.state;
}

int main() {
  return (int)(0ULL);

}

