/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct MultiLock {
  uint64_t lock_a;
  uint64_t lock_b;
  uint64_t lock_c;
  uint64_t held_count;
  uint64_t acquires;
  uint64_t releases;
} MultiLock;

/* Forward declarations */
MultiLock ml_new();
void ml_acquire_a(MultiLock* m __attribute__((unused)));
void ml_acquire_b(MultiLock* m __attribute__((unused)));
void ml_release_a(MultiLock* m __attribute__((unused)));
void ml_release_b(MultiLock* m __attribute__((unused)));
uint64_t hold_two();
uint64_t release_one();
uint64_t reacquire();
uint64_t release_count();
int main();

MultiLock ml_new() {
  return (MultiLock){ .lock_a = 0, .lock_b = 0, .lock_c = 0, .held_count = 0, .acquires = 0, .releases = 0 };
}

void ml_acquire_a(MultiLock* m __attribute__((unused))) {
  (*m).lock_a = 1;
  (*m).held_count = ((*m).held_count + 1);
  (*m).acquires = ((*m).acquires + 1);
}

void ml_acquire_b(MultiLock* m __attribute__((unused))) {
  (*m).lock_b = 1;
  (*m).held_count = ((*m).held_count + 1);
  (*m).acquires = ((*m).acquires + 1);
}

void ml_release_a(MultiLock* m __attribute__((unused))) {
  (*m).lock_a = 0;
  (*m).held_count = ((*m).held_count - 1);
  (*m).releases = ((*m).releases + 1);
}

void ml_release_b(MultiLock* m __attribute__((unused))) {
  (*m).lock_b = 0;
  (*m).held_count = ((*m).held_count - 1);
  (*m).releases = ((*m).releases + 1);
}

uint64_t hold_two() {
  MultiLock m __attribute__((unused)) = ml_new();
  ml_acquire_a((&m));
  ml_acquire_b((&m));
  return m.held_count;
}

uint64_t release_one() {
  MultiLock m __attribute__((unused)) = ml_new();
  ml_acquire_a((&m));
  ml_acquire_b((&m));
  ml_release_a((&m));
  return m.held_count;
}

uint64_t reacquire() {
  MultiLock m __attribute__((unused)) = ml_new();
  ml_acquire_a((&m));
  ml_release_a((&m));
  ml_acquire_a((&m));
  return m.lock_a;
}

uint64_t release_count() {
  MultiLock m __attribute__((unused)) = ml_new();
  ml_acquire_a((&m));
  ml_acquire_b((&m));
  ml_release_b((&m));
  ml_release_a((&m));
  return m.releases;
}

int main() {
  return (int)(0);

}

