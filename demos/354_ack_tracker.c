/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct AckTracker {
  uint64_t next_send;
  uint64_t ack_up_to;
  uint64_t window;
} AckTracker;

/* Forward declarations */
AckTracker ack_new(uint64_t window __attribute__((unused)));
void ack_send(AckTracker* a __attribute__((unused)));
void ack_ack(AckTracker* a __attribute__((unused)));
uint64_t send_three(uint64_t w __attribute__((unused)));
uint64_t in_flight(uint64_t w __attribute__((unused)));
uint64_t fully_acked(uint64_t w __attribute__((unused)));
uint64_t window_preserved(uint64_t w __attribute__((unused)));
int main();

AckTracker ack_new(uint64_t window __attribute__((unused))) {
  return (AckTracker){ .next_send = 0, .ack_up_to = 0, .window = window };
}

void ack_send(AckTracker* a __attribute__((unused))) {
  (*a).next_send = ((*a).next_send + 1);
}

void ack_ack(AckTracker* a __attribute__((unused))) {
  (*a).ack_up_to = ((*a).ack_up_to + 1);
}

uint64_t send_three(uint64_t w __attribute__((unused))) {
  AckTracker a __attribute__((unused)) = ack_new(w);
  ack_send((&a));
  ack_send((&a));
  ack_send((&a));
  return a.next_send;
}

uint64_t in_flight(uint64_t w __attribute__((unused))) {
  AckTracker a __attribute__((unused)) = ack_new(w);
  ack_send((&a));
  ack_send((&a));
  ack_send((&a));
  ack_ack((&a));
  ack_ack((&a));
  return (a.next_send - a.ack_up_to);
}

uint64_t fully_acked(uint64_t w __attribute__((unused))) {
  AckTracker a __attribute__((unused)) = ack_new(w);
  ack_send((&a));
  ack_send((&a));
  ack_send((&a));
  ack_ack((&a));
  ack_ack((&a));
  ack_ack((&a));
  return (a.next_send - a.ack_up_to);
}

uint64_t window_preserved(uint64_t w __attribute__((unused))) {
  AckTracker a __attribute__((unused)) = ack_new(w);
  ack_send((&a));
  ack_send((&a));
  ack_ack((&a));
  return a.window;
}

int main() {
  return (int)(0);

}

