/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct Retry {
  uint64_t attempts;
  uint64_t max_tries;
  uint64_t succeeded;
} Retry;

/* Forward declarations */
Retry retry_new(uint64_t max_tries __attribute__((unused)));
void retry_attempt(Retry* r __attribute__((unused)));
void retry_succeed(Retry* r __attribute__((unused)));
void retry_exhaust(Retry* r __attribute__((unused)));
uint64_t one_attempt(uint64_t max __attribute__((unused)));
uint64_t two_attempts(uint64_t max __attribute__((unused)));
uint64_t attempt_then_succeed(uint64_t max __attribute__((unused)));
uint64_t exhaust_after_one(uint64_t max __attribute__((unused)));
int main();

Retry retry_new(uint64_t max_tries __attribute__((unused))) {
  return (Retry){ .attempts = 0, .max_tries = max_tries, .succeeded = 0 };
}

void retry_attempt(Retry* r __attribute__((unused))) {
  (*r).attempts = ((*r).attempts + 1);
}

void retry_succeed(Retry* r __attribute__((unused))) {
  (*r).succeeded = 1;
}

void retry_exhaust(Retry* r __attribute__((unused))) {
  (*r).succeeded = 2;
}

uint64_t one_attempt(uint64_t max __attribute__((unused))) {
  Retry r __attribute__((unused)) = retry_new(max);
  retry_attempt((&r));
  return r.attempts;
}

uint64_t two_attempts(uint64_t max __attribute__((unused))) {
  Retry r __attribute__((unused)) = retry_new(max);
  retry_attempt((&r));
  retry_attempt((&r));
  return r.attempts;
}

uint64_t attempt_then_succeed(uint64_t max __attribute__((unused))) {
  Retry r __attribute__((unused)) = retry_new(max);
  retry_attempt((&r));
  retry_succeed((&r));
  return r.succeeded;
}

uint64_t exhaust_after_one(uint64_t max __attribute__((unused))) {
  Retry r __attribute__((unused)) = retry_new(max);
  retry_attempt((&r));
  retry_exhaust((&r));
  return r.succeeded;
}

int main() {
  return (int)(0);

}

