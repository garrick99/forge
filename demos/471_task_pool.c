/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct TaskPool {
  uint64_t high_q;
  uint64_t med_q;
  uint64_t low_q;
  uint64_t total;
  uint64_t steals;
} TaskPool;

/* Forward declarations */
TaskPool tp_new();
void tp_submit_high(TaskPool* t __attribute__((unused)));
void tp_submit_low(TaskPool* t __attribute__((unused)));
void tp_steal_low_to_high(TaskPool* t __attribute__((unused)), uint64_t n __attribute__((unused)));
void tp_complete_high(TaskPool* t __attribute__((unused)));
uint64_t submit_high();
uint64_t steal_to_high();
uint64_t total_conserved();
uint64_t steal_count();
int main();

TaskPool tp_new() {
  return (TaskPool){ .high_q = 0, .med_q = 0, .low_q = 0, .total = 0, .steals = 0 };
}

void tp_submit_high(TaskPool* t __attribute__((unused))) {
  (*t).high_q = ((*t).high_q + 1);
  (*t).total = ((*t).total + 1);
}

void tp_submit_low(TaskPool* t __attribute__((unused))) {
  (*t).low_q = ((*t).low_q + 1);
  (*t).total = ((*t).total + 1);
}

void tp_steal_low_to_high(TaskPool* t __attribute__((unused)), uint64_t n __attribute__((unused))) {
  (*t).low_q = ((*t).low_q - n);
  (*t).high_q = ((*t).high_q + n);
  (*t).steals = ((*t).steals + 1);
}

void tp_complete_high(TaskPool* t __attribute__((unused))) {
  (*t).high_q = ((*t).high_q - 1);
  (*t).total = ((*t).total - 1);
}

uint64_t submit_high() {
  TaskPool t __attribute__((unused)) = tp_new();
  tp_submit_high((&t));
  tp_submit_high((&t));
  tp_submit_high((&t));
  return t.high_q;
}

uint64_t steal_to_high() {
  TaskPool t __attribute__((unused)) = tp_new();
  tp_submit_high((&t));
  tp_submit_high((&t));
  tp_submit_low((&t));
  tp_submit_low((&t));
  tp_submit_low((&t));
  tp_steal_low_to_high((&t), 2);
  return t.high_q;
}

uint64_t total_conserved() {
  TaskPool t __attribute__((unused)) = tp_new();
  tp_submit_high((&t));
  tp_submit_low((&t));
  tp_submit_low((&t));
  tp_submit_low((&t));
  tp_submit_low((&t));
  tp_steal_low_to_high((&t), 2);
  return t.total;
}

uint64_t steal_count() {
  TaskPool t __attribute__((unused)) = tp_new();
  tp_submit_low((&t));
  tp_submit_low((&t));
  tp_submit_low((&t));
  tp_submit_low((&t));
  tp_steal_low_to_high((&t), 1);
  tp_steal_low_to_high((&t), 1);
  return t.steals;
}

int main() {
  return (int)(0);

}

