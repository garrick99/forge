/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct Conn {
  uint64_t state;
  uint64_t port;
} Conn;

/* Forward declarations */
Conn conn_new();
void conn_listen(Conn* c __attribute__((unused)), uint64_t port __attribute__((unused)));
void conn_accept(Conn* c __attribute__((unused)));
void conn_close(Conn* c __attribute__((unused)));
uint64_t full_cycle(uint64_t port __attribute__((unused)));
uint64_t reopen_cycle(uint64_t port1 __attribute__((unused)), uint64_t port2 __attribute__((unused)));
int main();

Conn conn_new() {
  return (Conn){ .state = 0, .port = 0 };
}

void conn_listen(Conn* c __attribute__((unused)), uint64_t port __attribute__((unused))) {
  (*c).state = 1;
  (*c).port = port;
}

void conn_accept(Conn* c __attribute__((unused))) {
  (*c).state = 2;
}

void conn_close(Conn* c __attribute__((unused))) {
  (*c).state = 0;
  (*c).port = 0;
}

uint64_t full_cycle(uint64_t port __attribute__((unused))) {
  Conn c __attribute__((unused)) = conn_new();
  conn_listen((&c), port);
  conn_accept((&c));
  conn_close((&c));
  return c.state;
}

uint64_t reopen_cycle(uint64_t port1 __attribute__((unused)), uint64_t port2 __attribute__((unused))) {
  Conn c __attribute__((unused)) = conn_new();
  conn_listen((&c), port1);
  conn_accept((&c));
  conn_close((&c));
  conn_listen((&c), port2);
  return c.state;
}

int main() {
  return (int)(0);

}

