/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

/* span<T> typedefs — fat pointers with proven bounds */
typedef struct { uint64_t* data; uintptr_t len; } forge_span_u64_t;

typedef struct MinMax {
  uint64_t min;
  uint64_t max;
  uint64_t count;
} MinMax;

/* Forward declarations */
MinMax minmax_new(uint64_t first __attribute__((unused)));
void minmax_update(MinMax* mm __attribute__((unused)), uint64_t v __attribute__((unused)));
uint64_t minmax_range(const MinMax* mm __attribute__((unused)));
uint64_t minmax_min(const MinMax* mm __attribute__((unused)));
uint64_t minmax_max(const MinMax* mm __attribute__((unused)));
MinMax span_minmax(forge_span_u64_t s __attribute__((unused)), uint64_t n __attribute__((unused)));
int main();

MinMax minmax_new(uint64_t first __attribute__((unused))) {
  return (MinMax){ .min = first, .max = first, .count = 1 };
}

void minmax_update(MinMax* mm __attribute__((unused)), uint64_t v __attribute__((unused))) {
  if ((v < (*mm).min)) {
    (*mm).min = v;

  }
  if ((v > (*mm).max)) {
    (*mm).max = v;

  }
  (*mm).count = ((*mm).count + 1);
}

uint64_t minmax_range(const MinMax* mm __attribute__((unused))) {
  return ((*mm).max - (*mm).min);
}

uint64_t minmax_min(const MinMax* mm __attribute__((unused))) {
  return (*mm).min;
}

uint64_t minmax_max(const MinMax* mm __attribute__((unused))) {
  return (*mm).max;
}

MinMax span_minmax(forge_span_u64_t s __attribute__((unused)), uint64_t n __attribute__((unused))) {
  MinMax mm __attribute__((unused)) = minmax_new(s.data[0]);
  uint64_t i __attribute__((unused)) = 1;
  {
    while ((i < n)) {
      minmax_update((&mm), s.data[i]);
      i = (i + 1);
    }

  }
  return mm;
}

int main() {
  return (int)(0);

}

