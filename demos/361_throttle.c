/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct Throttle {
  uint64_t tokens;
  uint64_t cap;
  uint64_t used;
} Throttle;

/* Forward declarations */
Throttle throttle_new(uint64_t cap __attribute__((unused)));
void throttle_consume(Throttle* t __attribute__((unused)), uint64_t n __attribute__((unused)));
void throttle_refill(Throttle* t __attribute__((unused)), uint64_t n __attribute__((unused)));
uint64_t consume_30();
uint64_t consumed_used();
uint64_t consume_refill();
uint64_t refill_no_affect_used();
uint64_t multi_consume();
int main();

Throttle throttle_new(uint64_t cap __attribute__((unused))) {
  return (Throttle){ .tokens = cap, .cap = cap, .used = 0 };
}

void throttle_consume(Throttle* t __attribute__((unused)), uint64_t n __attribute__((unused))) {
  (*t).tokens = ((*t).tokens - n);
  (*t).used = ((*t).used + n);
}

void throttle_refill(Throttle* t __attribute__((unused)), uint64_t n __attribute__((unused))) {
  (*t).tokens = ((*t).tokens + n);
}

uint64_t consume_30() {
  Throttle t __attribute__((unused)) = throttle_new(100);
  throttle_consume((&t), 30);
  return t.tokens;
}

uint64_t consumed_used() {
  Throttle t __attribute__((unused)) = throttle_new(100);
  throttle_consume((&t), 30);
  return t.used;
}

uint64_t consume_refill() {
  Throttle t __attribute__((unused)) = throttle_new(100);
  throttle_consume((&t), 50);
  throttle_refill((&t), 20);
  return t.tokens;
}

uint64_t refill_no_affect_used() {
  Throttle t __attribute__((unused)) = throttle_new(100);
  throttle_consume((&t), 50);
  throttle_refill((&t), 20);
  return t.used;
}

uint64_t multi_consume() {
  Throttle t __attribute__((unused)) = throttle_new(100);
  throttle_consume((&t), 10);
  throttle_consume((&t), 20);
  throttle_consume((&t), 15);
  return t.used;
}

int main() {
  return (int)(0);

}

