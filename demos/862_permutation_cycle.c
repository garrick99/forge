/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

/* span<T> typedefs — fat pointers with proven bounds */
typedef struct { uint64_t* data; uintptr_t len; } forge_span_u64_t;

/* Forward declarations */
uint64_t cycle_length(forge_span_u64_t perm __attribute__((unused)), uint64_t n __attribute__((unused)), uint64_t start __attribute__((unused)));
int main();

uint64_t cycle_length(forge_span_u64_t perm __attribute__((unused)), uint64_t n __attribute__((unused)), uint64_t start __attribute__((unused))) {
  uint64_t first __attribute__((unused)) = perm.data[start];
  uint64_t cur;
  if ((first < n)) {
    cur = first;
  } else {
    cur = start;
  }
  uint64_t len __attribute__((unused)) = 1;
  {
    while (((cur != start) && (len < n))) {
      uint64_t next __attribute__((unused)) = perm.data[cur];
      if ((next < n)) {
        cur = next;

      } else {
        cur = start;

      }
      if ((len < n)) {
        len = (len + 1);

      }
    }

  }
  return len;
}

int main() {
  return (int)(0);

}

