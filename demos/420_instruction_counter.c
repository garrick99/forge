/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct InstrCounter {
  uint64_t loads;
  uint64_t stores;
  uint64_t arith;
  uint64_t branches;
} InstrCounter;

/* Forward declarations */
InstrCounter ic_new();
void ic_load(InstrCounter* c __attribute__((unused)));
void ic_store(InstrCounter* c __attribute__((unused)));
void ic_arith(InstrCounter* c __attribute__((unused)));
void ic_branch(InstrCounter* c __attribute__((unused)));
uint64_t mixed_total();
uint64_t memory_ops();
uint64_t branch_count();
uint64_t arith_no_load_effect();
int main();

InstrCounter ic_new() {
  return (InstrCounter){ .loads = 0, .stores = 0, .arith = 0, .branches = 0 };
}

void ic_load(InstrCounter* c __attribute__((unused))) {
  (*c).loads = ((*c).loads + 1);
}

void ic_store(InstrCounter* c __attribute__((unused))) {
  (*c).stores = ((*c).stores + 1);
}

void ic_arith(InstrCounter* c __attribute__((unused))) {
  (*c).arith = ((*c).arith + 1);
}

void ic_branch(InstrCounter* c __attribute__((unused))) {
  (*c).branches = ((*c).branches + 1);
}

uint64_t mixed_total() {
  InstrCounter c __attribute__((unused)) = ic_new();
  ic_load((&c));
  ic_load((&c));
  ic_load((&c));
  ic_store((&c));
  ic_store((&c));
  ic_arith((&c));
  ic_arith((&c));
  ic_arith((&c));
  ic_branch((&c));
  ic_branch((&c));
  return (((c.loads + c.stores) + c.arith) + c.branches);
}

uint64_t memory_ops() {
  InstrCounter c __attribute__((unused)) = ic_new();
  ic_load((&c));
  ic_load((&c));
  ic_load((&c));
  ic_store((&c));
  ic_store((&c));
  ic_arith((&c));
  ic_branch((&c));
  return (c.loads + c.stores);
}

uint64_t branch_count() {
  InstrCounter c __attribute__((unused)) = ic_new();
  ic_arith((&c));
  ic_branch((&c));
  ic_branch((&c));
  ic_arith((&c));
  ic_branch((&c));
  ic_branch((&c));
  return c.branches;
}

uint64_t arith_no_load_effect() {
  InstrCounter c __attribute__((unused)) = ic_new();
  ic_load((&c));
  ic_load((&c));
  ic_arith((&c));
  ic_arith((&c));
  ic_arith((&c));
  return c.loads;
}

int main() {
  return (int)(0);

}

