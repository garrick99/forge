/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct WAL {
  uint64_t write_lsn;
  uint64_t commit_lsn;
  uint64_t commits;
  uint64_t rollbacks;
  uint64_t entries;
} WAL;

/* Forward declarations */
WAL wal_new();
void wal_write(WAL* w __attribute__((unused)));
void wal_commit(WAL* w __attribute__((unused)));
void wal_rollback(WAL* w __attribute__((unused)));
uint64_t write_3();
uint64_t commit_advances();
uint64_t rollback_reverts();
uint64_t commit_count();
int main();

WAL wal_new() {
  return (WAL){ .write_lsn = 0ULL, .commit_lsn = 0ULL, .commits = 0ULL, .rollbacks = 0ULL, .entries = 0ULL };
}

void wal_write(WAL* w __attribute__((unused))) {
  (*w).write_lsn = ((*w).write_lsn + 1ULL);
  (*w).entries = ((*w).entries + 1ULL);
}

void wal_commit(WAL* w __attribute__((unused))) {
  (*w).commit_lsn = (*w).write_lsn;
  (*w).commits = ((*w).commits + 1ULL);
}

void wal_rollback(WAL* w __attribute__((unused))) {
  (*w).write_lsn = (*w).commit_lsn;
  (*w).rollbacks = ((*w).rollbacks + 1ULL);
}

uint64_t write_3() {
  WAL w __attribute__((unused)) = wal_new();
  wal_write((&w));
  wal_write((&w));
  wal_write((&w));
  return w.write_lsn;
}

uint64_t commit_advances() {
  WAL w __attribute__((unused)) = wal_new();
  wal_write((&w));
  wal_write((&w));
  wal_write((&w));
  wal_commit((&w));
  return w.commit_lsn;
}

uint64_t rollback_reverts() {
  WAL w __attribute__((unused)) = wal_new();
  wal_write((&w));
  wal_write((&w));
  wal_commit((&w));
  wal_write((&w));
  wal_write((&w));
  wal_rollback((&w));
  return w.write_lsn;
}

uint64_t commit_count() {
  WAL w __attribute__((unused)) = wal_new();
  wal_write((&w));
  wal_commit((&w));
  wal_write((&w));
  wal_commit((&w));
  return w.commits;
}

int main() {
  return (int)(0ULL);

}

