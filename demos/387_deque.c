/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct Deque {
  uint64_t len;
  uint64_t cap;
} Deque;

/* Forward declarations */
Deque deque_new(uint64_t cap __attribute__((unused)));
void deque_push_front(Deque* d __attribute__((unused)));
void deque_push_back(Deque* d __attribute__((unused)));
void deque_pop_front(Deque* d __attribute__((unused)));
void deque_pop_back(Deque* d __attribute__((unused)));
uint64_t push_front_three(uint64_t cap __attribute__((unused)));
uint64_t push_mixed(uint64_t cap __attribute__((unused)));
uint64_t push4_pop2(uint64_t cap __attribute__((unused)));
uint64_t cap_preserved(uint64_t cap __attribute__((unused)));
int main();

Deque deque_new(uint64_t cap __attribute__((unused))) {
  return (Deque){ .len = 0ULL, .cap = cap };
}

void deque_push_front(Deque* d __attribute__((unused))) {
  (*d).len = ((*d).len + 1ULL);
}

void deque_push_back(Deque* d __attribute__((unused))) {
  (*d).len = ((*d).len + 1ULL);
}

void deque_pop_front(Deque* d __attribute__((unused))) {
  (*d).len = ((*d).len - 1ULL);
}

void deque_pop_back(Deque* d __attribute__((unused))) {
  (*d).len = ((*d).len - 1ULL);
}

uint64_t push_front_three(uint64_t cap __attribute__((unused))) {
  Deque d __attribute__((unused)) = deque_new(cap);
  deque_push_front((&d));
  deque_push_front((&d));
  deque_push_front((&d));
  return d.len;
}

uint64_t push_mixed(uint64_t cap __attribute__((unused))) {
  Deque d __attribute__((unused)) = deque_new(cap);
  deque_push_front((&d));
  deque_push_back((&d));
  deque_push_front((&d));
  deque_push_back((&d));
  return d.len;
}

uint64_t push4_pop2(uint64_t cap __attribute__((unused))) {
  Deque d __attribute__((unused)) = deque_new(cap);
  deque_push_front((&d));
  deque_push_back((&d));
  deque_push_front((&d));
  deque_push_back((&d));
  deque_pop_front((&d));
  deque_pop_back((&d));
  return d.len;
}

uint64_t cap_preserved(uint64_t cap __attribute__((unused))) {
  Deque d __attribute__((unused)) = deque_new(cap);
  deque_push_front((&d));
  deque_push_back((&d));
  deque_pop_front((&d));
  return d.cap;
}

int main() {
  return (int)(0ULL);

}

