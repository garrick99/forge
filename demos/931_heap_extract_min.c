/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

/* span<T> typedefs — fat pointers with proven bounds */
typedef struct { uint64_t* data; uintptr_t len; } forge_span_u64_t;

/* Forward declarations */
void sift_down(forge_span_u64_t heap __attribute__((unused)), uint64_t n __attribute__((unused)));
uint64_t extract_min(forge_span_u64_t heap __attribute__((unused)), uint64_t n __attribute__((unused)));
int main();

void sift_down(forge_span_u64_t heap __attribute__((unused)), uint64_t n __attribute__((unused))) {
  uint64_t i __attribute__((unused)) = 0;
  _Bool done __attribute__((unused)) = 0;
  {
    while ((done == 0)) {
      uint64_t left __attribute__((unused)) = ((2 * i) + 1);
      uint64_t right __attribute__((unused)) = ((2 * i) + 2);
      if ((left >= n)) {
        done = 1;

      } else {
        uint64_t child;
        if (((right < n) && (heap.data[right] < heap.data[left]))) {
          child = right;
        } else {
          child = left;
        }
        if ((heap.data[child] < heap.data[i])) {
          uint64_t tmp __attribute__((unused)) = heap.data[i];
          heap.data[i] = heap.data[child];
          heap.data[child] = tmp;
          i = child;
          if ((((2 * i) + 1) >= n)) {
            done = 1;

          }

        } else {
          done = 1;

        }

      }
    }

  }
}

uint64_t extract_min(forge_span_u64_t heap __attribute__((unused)), uint64_t n __attribute__((unused))) {
  uint64_t min_val __attribute__((unused)) = heap.data[0];
  heap.data[0] = heap.data[(n - 1)];
  if ((n > 1)) {
    sift_down(heap, (n - 1));

  }
  return min_val;
}

int main() {
  return (int)(0);

}

