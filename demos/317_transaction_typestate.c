/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct Txn {
  uint64_t state;
  uint64_t ops;
  uint64_t version;
} Txn;

/* Forward declarations */
Txn txn_new();
void txn_begin(Txn* t __attribute__((unused)));
void txn_write(Txn* t __attribute__((unused)));
void txn_commit(Txn* t __attribute__((unused)));
void txn_rollback(Txn* t __attribute__((unused)));
uint64_t commit_cycle();
uint64_t rollback_cycle();
uint64_t two_commits();
uint64_t retry_cycle();
int main();

Txn txn_new() {
  return (Txn){ .state = 0, .ops = 0, .version = 0 };
}

void txn_begin(Txn* t __attribute__((unused))) {
  (*t).state = 1;
  (*t).ops = 0;
}

void txn_write(Txn* t __attribute__((unused))) {
  (*t).ops = ((*t).ops + 1);
}

void txn_commit(Txn* t __attribute__((unused))) {
  (*t).state = 0;
  (*t).version = ((*t).version + 1);
}

void txn_rollback(Txn* t __attribute__((unused))) {
  (*t).state = 0;
  (*t).ops = 0;
}

uint64_t commit_cycle() {
  Txn t __attribute__((unused)) = txn_new();
  txn_begin((&t));
  txn_write((&t));
  txn_commit((&t));
  return t.state;
}

uint64_t rollback_cycle() {
  Txn t __attribute__((unused)) = txn_new();
  txn_begin((&t));
  txn_write((&t));
  txn_rollback((&t));
  return t.state;
}

uint64_t two_commits() {
  Txn t __attribute__((unused)) = txn_new();
  txn_begin((&t));
  txn_commit((&t));
  txn_begin((&t));
  txn_write((&t));
  txn_commit((&t));
  return t.state;
}

uint64_t retry_cycle() {
  Txn t __attribute__((unused)) = txn_new();
  txn_begin((&t));
  txn_write((&t));
  txn_rollback((&t));
  txn_begin((&t));
  txn_write((&t));
  txn_commit((&t));
  return t.state;
}

int main() {
  return (int)(0);

}

