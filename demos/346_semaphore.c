/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct Semaphore {
  uint64_t count;
  uint64_t cap;
} Semaphore;

/* Forward declarations */
Semaphore sem_new(uint64_t cap __attribute__((unused)));
void sem_down(Semaphore* s __attribute__((unused)));
void sem_up(Semaphore* s __attribute__((unused)));
uint64_t initial_count();
uint64_t three_downs();
uint64_t down_up();
uint64_t two_down_one_up(uint64_t cap __attribute__((unused)));
int main();

Semaphore sem_new(uint64_t cap __attribute__((unused))) {
  return (Semaphore){ .count = cap, .cap = cap };
}

void sem_down(Semaphore* s __attribute__((unused))) {
  (*s).count = ((*s).count - 1);
}

void sem_up(Semaphore* s __attribute__((unused))) {
  (*s).count = ((*s).count + 1);
}

uint64_t initial_count() {
  Semaphore s __attribute__((unused)) = sem_new(3);
  return s.count;
}

uint64_t three_downs() {
  Semaphore s __attribute__((unused)) = sem_new(3);
  sem_down((&s));
  sem_down((&s));
  sem_down((&s));
  return s.count;
}

uint64_t down_up() {
  Semaphore s __attribute__((unused)) = sem_new(5);
  sem_down((&s));
  sem_up((&s));
  return s.count;
}

uint64_t two_down_one_up(uint64_t cap __attribute__((unused))) {
  Semaphore s __attribute__((unused)) = sem_new(cap);
  sem_down((&s));
  sem_down((&s));
  sem_up((&s));
  return s.count;
}

int main() {
  return (int)(0);

}

