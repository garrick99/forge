/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct CacheStats {
  uint64_t hits;
  uint64_t misses;
} CacheStats;

/* Forward declarations */
CacheStats cs_new();
void cs_hit(CacheStats* c __attribute__((unused)));
void cs_miss(CacheStats* c __attribute__((unused)));
uint64_t five_hits();
uint64_t total_accesses();
uint64_t misses_independent();
uint64_t hits_independent();
uint64_t all_misses();
int main();

CacheStats cs_new() {
  return (CacheStats){ .hits = 0ULL, .misses = 0ULL };
}

void cs_hit(CacheStats* c __attribute__((unused))) {
  (*c).hits = ((*c).hits + 1ULL);
}

void cs_miss(CacheStats* c __attribute__((unused))) {
  (*c).misses = ((*c).misses + 1ULL);
}

uint64_t five_hits() {
  CacheStats c __attribute__((unused)) = cs_new();
  cs_hit((&c));
  cs_hit((&c));
  cs_hit((&c));
  cs_hit((&c));
  cs_hit((&c));
  return c.hits;
}

uint64_t total_accesses() {
  CacheStats c __attribute__((unused)) = cs_new();
  cs_hit((&c));
  cs_hit((&c));
  cs_hit((&c));
  cs_miss((&c));
  cs_miss((&c));
  return (c.hits + c.misses);
}

uint64_t misses_independent() {
  CacheStats c __attribute__((unused)) = cs_new();
  cs_hit((&c));
  cs_hit((&c));
  cs_hit((&c));
  cs_miss((&c));
  cs_miss((&c));
  return c.hits;
}

uint64_t hits_independent() {
  CacheStats c __attribute__((unused)) = cs_new();
  cs_hit((&c));
  cs_hit((&c));
  cs_hit((&c));
  cs_miss((&c));
  cs_miss((&c));
  return c.misses;
}

uint64_t all_misses() {
  CacheStats c __attribute__((unused)) = cs_new();
  cs_miss((&c));
  cs_miss((&c));
  cs_miss((&c));
  return c.hits;
}

int main() {
  return (int)(0ULL);

}

