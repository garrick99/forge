/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct Lock {
  uint64_t state;
} Lock;

/* Forward declarations */
Lock lock_new();
void lock_acquire(Lock* l __attribute__((unused)));
void lock_release(Lock* l __attribute__((unused)));
uint64_t lock_is_held(Lock* l __attribute__((unused)));
uint64_t critical_section(uint64_t work __attribute__((unused)));
uint64_t two_critical_sections(uint64_t a __attribute__((unused)), uint64_t b __attribute__((unused)));
uint64_t locked_read(uint64_t data __attribute__((unused)));
int main();

Lock lock_new() {
  return (Lock){ .state = 0ULL };
}

void lock_acquire(Lock* l __attribute__((unused))) {
  (*l).state = 1ULL;
}

void lock_release(Lock* l __attribute__((unused))) {
  (*l).state = 0ULL;
}

uint64_t lock_is_held(Lock* l __attribute__((unused))) {
  return (*l).state;
}

uint64_t critical_section(uint64_t work __attribute__((unused))) {
  Lock l __attribute__((unused)) = lock_new();
  lock_acquire((&l));
  (void)(work);
  lock_release((&l));
  return l.state;
}

uint64_t two_critical_sections(uint64_t a __attribute__((unused)), uint64_t b __attribute__((unused))) {
  Lock l __attribute__((unused)) = lock_new();
  lock_acquire((&l));
  (void)(a);
  lock_release((&l));
  lock_acquire((&l));
  (void)(b);
  lock_release((&l));
  return l.state;
}

uint64_t locked_read(uint64_t data __attribute__((unused))) {
  Lock l __attribute__((unused)) = lock_new();
  lock_acquire((&l));
  uint64_t v __attribute__((unused)) = data;
  lock_release((&l));
  return v;
}

int main() {
  return (int)(0ULL);

}

