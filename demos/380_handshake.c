/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct Handshake {
  uint64_t state;
  uint64_t retries;
} Handshake;

/* Forward declarations */
Handshake hs_new();
void hs_send_syn(Handshake* h __attribute__((unused)));
void hs_recv_syn_ack(Handshake* h __attribute__((unused)));
void hs_send_ack(Handshake* h __attribute__((unused)));
void hs_retry(Handshake* h __attribute__((unused)));
uint64_t after_syn();
uint64_t after_syn_ack();
uint64_t established();
uint64_t retry_then_establish_retries();
uint64_t retry_then_establish_state();
int main();

Handshake hs_new() {
  return (Handshake){ .state = 0, .retries = 0 };
}

void hs_send_syn(Handshake* h __attribute__((unused))) {
  (*h).state = 1;
}

void hs_recv_syn_ack(Handshake* h __attribute__((unused))) {
  (*h).state = 2;
}

void hs_send_ack(Handshake* h __attribute__((unused))) {
  (*h).state = 3;
}

void hs_retry(Handshake* h __attribute__((unused))) {
  (*h).state = 0;
  (*h).retries = ((*h).retries + 1);
}

uint64_t after_syn() {
  Handshake h __attribute__((unused)) = hs_new();
  hs_send_syn((&h));
  return h.state;
}

uint64_t after_syn_ack() {
  Handshake h __attribute__((unused)) = hs_new();
  hs_send_syn((&h));
  hs_recv_syn_ack((&h));
  return h.state;
}

uint64_t established() {
  Handshake h __attribute__((unused)) = hs_new();
  hs_send_syn((&h));
  hs_recv_syn_ack((&h));
  hs_send_ack((&h));
  return h.state;
}

uint64_t retry_then_establish_retries() {
  Handshake h __attribute__((unused)) = hs_new();
  hs_send_syn((&h));
  hs_retry((&h));
  hs_send_syn((&h));
  hs_recv_syn_ack((&h));
  hs_send_ack((&h));
  return h.retries;
}

uint64_t retry_then_establish_state() {
  Handshake h __attribute__((unused)) = hs_new();
  hs_send_syn((&h));
  hs_retry((&h));
  hs_send_syn((&h));
  hs_recv_syn_ack((&h));
  hs_send_ack((&h));
  return h.state;
}

int main() {
  return (int)(0);

}

