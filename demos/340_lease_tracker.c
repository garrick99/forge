/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct Lease {
  uint64_t acquired;
  uint64_t released;
} Lease;

/* Forward declarations */
Lease lease_new();
void lease_acquire(Lease* l __attribute__((unused)));
void lease_release(Lease* l __attribute__((unused)));
uint64_t one_acquire();
uint64_t three_acquires();
uint64_t acquire_release();
uint64_t outstanding();
uint64_t fully_released();
int main();

Lease lease_new() {
  return (Lease){ .acquired = 0ULL, .released = 0ULL };
}

void lease_acquire(Lease* l __attribute__((unused))) {
  (*l).acquired = ((*l).acquired + 1ULL);
}

void lease_release(Lease* l __attribute__((unused))) {
  (*l).released = ((*l).released + 1ULL);
}

uint64_t one_acquire() {
  Lease l __attribute__((unused)) = lease_new();
  lease_acquire((&l));
  return l.acquired;
}

uint64_t three_acquires() {
  Lease l __attribute__((unused)) = lease_new();
  lease_acquire((&l));
  lease_acquire((&l));
  lease_acquire((&l));
  return l.acquired;
}

uint64_t acquire_release() {
  Lease l __attribute__((unused)) = lease_new();
  lease_acquire((&l));
  lease_release((&l));
  return l.released;
}

uint64_t outstanding() {
  Lease l __attribute__((unused)) = lease_new();
  lease_acquire((&l));
  lease_acquire((&l));
  lease_acquire((&l));
  lease_release((&l));
  lease_release((&l));
  return (l.acquired - l.released);
}

uint64_t fully_released() {
  Lease l __attribute__((unused)) = lease_new();
  lease_acquire((&l));
  lease_acquire((&l));
  lease_release((&l));
  lease_release((&l));
  return (l.acquired - l.released);
}

int main() {
  return (int)(0ULL);

}

