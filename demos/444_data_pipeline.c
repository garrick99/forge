/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct DataPipeline {
  uint64_t ingested;
  uint64_t accepted;
  uint64_t rejected;
  uint64_t processed;
} DataPipeline;

/* Forward declarations */
DataPipeline dp_new();
void dp_ingest_accept(DataPipeline* d __attribute__((unused)));
void dp_ingest_reject(DataPipeline* d __attribute__((unused)));
void dp_process(DataPipeline* d __attribute__((unused)));
uint64_t ingest_count();
uint64_t conservation();
uint64_t process_count();
uint64_t rejected_not_processed();
int main();

DataPipeline dp_new() {
  return (DataPipeline){ .ingested = 0, .accepted = 0, .rejected = 0, .processed = 0 };
}

void dp_ingest_accept(DataPipeline* d __attribute__((unused))) {
  (*d).ingested = ((*d).ingested + 1);
  (*d).accepted = ((*d).accepted + 1);
}

void dp_ingest_reject(DataPipeline* d __attribute__((unused))) {
  (*d).ingested = ((*d).ingested + 1);
  (*d).rejected = ((*d).rejected + 1);
}

void dp_process(DataPipeline* d __attribute__((unused))) {
  (*d).processed = ((*d).processed + 1);
}

uint64_t ingest_count() {
  DataPipeline d __attribute__((unused)) = dp_new();
  dp_ingest_accept((&d));
  dp_ingest_accept((&d));
  dp_ingest_accept((&d));
  dp_ingest_reject((&d));
  return d.ingested;
}

uint64_t conservation() {
  DataPipeline d __attribute__((unused)) = dp_new();
  dp_ingest_accept((&d));
  dp_ingest_reject((&d));
  dp_ingest_accept((&d));
  dp_ingest_reject((&d));
  return ((d.ingested - d.accepted) - d.rejected);
}

uint64_t process_count() {
  DataPipeline d __attribute__((unused)) = dp_new();
  dp_ingest_accept((&d));
  dp_ingest_accept((&d));
  dp_ingest_accept((&d));
  dp_process((&d));
  dp_process((&d));
  return d.processed;
}

uint64_t rejected_not_processed() {
  DataPipeline d __attribute__((unused)) = dp_new();
  dp_ingest_reject((&d));
  dp_ingest_reject((&d));
  dp_ingest_reject((&d));
  return d.rejected;
}

int main() {
  return (int)(0);

}

