/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct SeqState {
  uint64_t next_seq;
  uint64_t sent;
} SeqState;

/* Forward declarations */
SeqState seq_init();
void seq_send(SeqState* s __attribute__((unused)));
uint64_t one_send_seq();
uint64_t one_send_count();
uint64_t three_sends();
uint64_t seq_equals_count();
int main();

SeqState seq_init() {
  return (SeqState){ .next_seq = 0, .sent = 0 };
}

void seq_send(SeqState* s __attribute__((unused))) {
  (*s).next_seq = ((*s).next_seq + 1);
  (*s).sent = ((*s).sent + 1);
}

uint64_t one_send_seq() {
  SeqState s __attribute__((unused)) = seq_init();
  seq_send((&s));
  return s.next_seq;
}

uint64_t one_send_count() {
  SeqState s __attribute__((unused)) = seq_init();
  seq_send((&s));
  return s.sent;
}

uint64_t three_sends() {
  SeqState s __attribute__((unused)) = seq_init();
  seq_send((&s));
  seq_send((&s));
  seq_send((&s));
  return s.next_seq;
}

uint64_t seq_equals_count() {
  SeqState s __attribute__((unused)) = seq_init();
  seq_send((&s));
  seq_send((&s));
  return (s.next_seq - s.sent);
}

int main() {
  return (int)(0);

}

