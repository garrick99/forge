/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct WorkQueue {
  uint64_t enqueued;
  uint64_t completed;
  uint64_t cap;
} WorkQueue;

/* Forward declarations */
WorkQueue wq_new(uint64_t cap __attribute__((unused)));
void wq_enqueue(WorkQueue* w __attribute__((unused)));
void wq_complete(WorkQueue* w __attribute__((unused)));
uint64_t enqueue_four(uint64_t cap __attribute__((unused)));
uint64_t enqueue3_complete2(uint64_t cap __attribute__((unused)));
uint64_t drain(uint64_t cap __attribute__((unused)));
uint64_t completed_count(uint64_t cap __attribute__((unused)));
int main();

WorkQueue wq_new(uint64_t cap __attribute__((unused))) {
  return (WorkQueue){ .enqueued = 0ULL, .completed = 0ULL, .cap = cap };
}

void wq_enqueue(WorkQueue* w __attribute__((unused))) {
  (*w).enqueued = ((*w).enqueued + 1ULL);
}

void wq_complete(WorkQueue* w __attribute__((unused))) {
  (*w).completed = ((*w).completed + 1ULL);
}

uint64_t enqueue_four(uint64_t cap __attribute__((unused))) {
  WorkQueue w __attribute__((unused)) = wq_new(cap);
  wq_enqueue((&w));
  wq_enqueue((&w));
  wq_enqueue((&w));
  wq_enqueue((&w));
  return (w.enqueued - w.completed);
}

uint64_t enqueue3_complete2(uint64_t cap __attribute__((unused))) {
  WorkQueue w __attribute__((unused)) = wq_new(cap);
  wq_enqueue((&w));
  wq_enqueue((&w));
  wq_enqueue((&w));
  wq_complete((&w));
  wq_complete((&w));
  return (w.enqueued - w.completed);
}

uint64_t drain(uint64_t cap __attribute__((unused))) {
  WorkQueue w __attribute__((unused)) = wq_new(cap);
  wq_enqueue((&w));
  wq_enqueue((&w));
  wq_complete((&w));
  wq_complete((&w));
  return (w.enqueued - w.completed);
}

uint64_t completed_count(uint64_t cap __attribute__((unused))) {
  WorkQueue w __attribute__((unused)) = wq_new(cap);
  wq_enqueue((&w));
  wq_enqueue((&w));
  wq_enqueue((&w));
  wq_complete((&w));
  wq_complete((&w));
  wq_complete((&w));
  return w.completed;
}

int main() {
  return (int)(0ULL);

}

