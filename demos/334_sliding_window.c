/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct Window {
  uint64_t send_base;
  uint64_t next_seq;
  uint64_t window;
} Window;

/* Forward declarations */
Window window_new(uint64_t window __attribute__((unused)));
void window_send(Window* w __attribute__((unused)));
void window_ack(Window* w __attribute__((unused)));
uint64_t send_one(uint64_t win __attribute__((unused)));
uint64_t send2_ack1_base(uint64_t win __attribute__((unused)));
uint64_t send2_ack1_next(uint64_t win __attribute__((unused)));
uint64_t send3_ack3(uint64_t win __attribute__((unused)));
int main();

Window window_new(uint64_t window __attribute__((unused))) {
  return (Window){ .send_base = 0, .next_seq = 0, .window = window };
}

void window_send(Window* w __attribute__((unused))) {
  (*w).next_seq = ((*w).next_seq + 1);
}

void window_ack(Window* w __attribute__((unused))) {
  (*w).send_base = ((*w).send_base + 1);
}

uint64_t send_one(uint64_t win __attribute__((unused))) {
  Window w __attribute__((unused)) = window_new(win);
  window_send((&w));
  return w.next_seq;
}

uint64_t send2_ack1_base(uint64_t win __attribute__((unused))) {
  Window w __attribute__((unused)) = window_new(win);
  window_send((&w));
  window_send((&w));
  window_ack((&w));
  return w.send_base;
}

uint64_t send2_ack1_next(uint64_t win __attribute__((unused))) {
  Window w __attribute__((unused)) = window_new(win);
  window_send((&w));
  window_send((&w));
  window_ack((&w));
  return w.next_seq;
}

uint64_t send3_ack3(uint64_t win __attribute__((unused))) {
  Window w __attribute__((unused)) = window_new(win);
  window_send((&w));
  window_send((&w));
  window_send((&w));
  window_ack((&w));
  window_ack((&w));
  window_ack((&w));
  return w.send_base;
}

int main() {
  return (int)(0);

}

