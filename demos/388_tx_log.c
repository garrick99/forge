/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct TxLog {
  uint64_t open;
  uint64_t committed;
  uint64_t aborted;
  uint64_t total;
} TxLog;

/* Forward declarations */
TxLog txlog_new();
void txlog_begin(TxLog* t __attribute__((unused)));
void txlog_commit(TxLog* t __attribute__((unused)));
void txlog_abort(TxLog* t __attribute__((unused)));
uint64_t begin_three_open();
uint64_t commit_two();
uint64_t mixed_ops_open();
uint64_t total_grows();
int main();

TxLog txlog_new() {
  return (TxLog){ .open = 0, .committed = 0, .aborted = 0, .total = 0 };
}

void txlog_begin(TxLog* t __attribute__((unused))) {
  (*t).open = ((*t).open + 1);
  (*t).total = ((*t).total + 1);
}

void txlog_commit(TxLog* t __attribute__((unused))) {
  (*t).committed = ((*t).committed + 1);
  (*t).open = ((*t).open - 1);
}

void txlog_abort(TxLog* t __attribute__((unused))) {
  (*t).aborted = ((*t).aborted + 1);
  (*t).open = ((*t).open - 1);
}

uint64_t begin_three_open() {
  TxLog t __attribute__((unused)) = txlog_new();
  txlog_begin((&t));
  txlog_begin((&t));
  txlog_begin((&t));
  return t.open;
}

uint64_t commit_two() {
  TxLog t __attribute__((unused)) = txlog_new();
  txlog_begin((&t));
  txlog_begin((&t));
  txlog_begin((&t));
  txlog_commit((&t));
  txlog_commit((&t));
  return t.committed;
}

uint64_t mixed_ops_open() {
  TxLog t __attribute__((unused)) = txlog_new();
  txlog_begin((&t));
  txlog_begin((&t));
  txlog_begin((&t));
  txlog_commit((&t));
  txlog_abort((&t));
  return t.open;
}

uint64_t total_grows() {
  TxLog t __attribute__((unused)) = txlog_new();
  txlog_begin((&t));
  txlog_begin((&t));
  txlog_commit((&t));
  txlog_begin((&t));
  txlog_abort((&t));
  txlog_begin((&t));
  return t.total;
}

int main() {
  return (int)(0);

}

