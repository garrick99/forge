/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct MsgBus {
  uint64_t published;
  uint64_t delivered;
  uint64_t subscribers;
} MsgBus;

/* Forward declarations */
MsgBus bus_new();
void bus_subscribe(MsgBus* b __attribute__((unused)));
void bus_publish(MsgBus* b __attribute__((unused)));
void bus_deliver(MsgBus* b __attribute__((unused)));
uint64_t three_subscribers();
uint64_t five_published();
uint64_t in_flight();
uint64_t all_delivered();
int main();

MsgBus bus_new() {
  return (MsgBus){ .published = 0, .delivered = 0, .subscribers = 0 };
}

void bus_subscribe(MsgBus* b __attribute__((unused))) {
  (*b).subscribers = ((*b).subscribers + 1);
}

void bus_publish(MsgBus* b __attribute__((unused))) {
  (*b).published = ((*b).published + 1);
}

void bus_deliver(MsgBus* b __attribute__((unused))) {
  (*b).delivered = ((*b).delivered + 1);
}

uint64_t three_subscribers() {
  MsgBus b __attribute__((unused)) = bus_new();
  bus_subscribe((&b));
  bus_subscribe((&b));
  bus_subscribe((&b));
  return b.subscribers;
}

uint64_t five_published() {
  MsgBus b __attribute__((unused)) = bus_new();
  bus_publish((&b));
  bus_publish((&b));
  bus_publish((&b));
  bus_publish((&b));
  bus_publish((&b));
  return b.published;
}

uint64_t in_flight() {
  MsgBus b __attribute__((unused)) = bus_new();
  bus_publish((&b));
  bus_publish((&b));
  bus_publish((&b));
  bus_deliver((&b));
  bus_deliver((&b));
  return (b.published - b.delivered);
}

uint64_t all_delivered() {
  MsgBus b __attribute__((unused)) = bus_new();
  bus_publish((&b));
  bus_publish((&b));
  bus_deliver((&b));
  bus_deliver((&b));
  return (b.published - b.delivered);
}

int main() {
  return (int)(0);

}

