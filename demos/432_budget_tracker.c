/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct Budget {
  uint64_t remaining;
  uint64_t spent_food;
  uint64_t spent_transport;
  uint64_t spent_other;
  uint64_t total_spent;
} Budget;

/* Forward declarations */
Budget budget_new(uint64_t amount __attribute__((unused)));
void budget_spend_food(Budget* b __attribute__((unused)), uint64_t n __attribute__((unused)));
void budget_spend_transport(Budget* b __attribute__((unused)), uint64_t n __attribute__((unused)));
void budget_spend_other(Budget* b __attribute__((unused)), uint64_t n __attribute__((unused)));
uint64_t food_spend();
uint64_t mixed_total();
uint64_t categories_sum();
uint64_t food_independent();
int main();

Budget budget_new(uint64_t amount __attribute__((unused))) {
  return (Budget){ .remaining = amount, .spent_food = 0, .spent_transport = 0, .spent_other = 0, .total_spent = 0 };
}

void budget_spend_food(Budget* b __attribute__((unused)), uint64_t n __attribute__((unused))) {
  (*b).remaining = ((*b).remaining - n);
  (*b).spent_food = ((*b).spent_food + n);
  (*b).total_spent = ((*b).total_spent + n);
}

void budget_spend_transport(Budget* b __attribute__((unused)), uint64_t n __attribute__((unused))) {
  (*b).remaining = ((*b).remaining - n);
  (*b).spent_transport = ((*b).spent_transport + n);
  (*b).total_spent = ((*b).total_spent + n);
}

void budget_spend_other(Budget* b __attribute__((unused)), uint64_t n __attribute__((unused))) {
  (*b).remaining = ((*b).remaining - n);
  (*b).spent_other = ((*b).spent_other + n);
  (*b).total_spent = ((*b).total_spent + n);
}

uint64_t food_spend() {
  Budget b __attribute__((unused)) = budget_new(100);
  budget_spend_food((&b), 30);
  return b.remaining;
}

uint64_t mixed_total() {
  Budget b __attribute__((unused)) = budget_new(100);
  budget_spend_food((&b), 20);
  budget_spend_transport((&b), 15);
  budget_spend_other((&b), 15);
  return b.total_spent;
}

uint64_t categories_sum() {
  Budget b __attribute__((unused)) = budget_new(100);
  budget_spend_food((&b), 20);
  budget_spend_transport((&b), 15);
  budget_spend_other((&b), 10);
  return (((b.total_spent - b.spent_food) - b.spent_transport) - b.spent_other);
}

uint64_t food_independent() {
  Budget b __attribute__((unused)) = budget_new(100);
  budget_spend_food((&b), 20);
  budget_spend_transport((&b), 30);
  return b.spent_food;
}

int main() {
  return (int)(0);

}

