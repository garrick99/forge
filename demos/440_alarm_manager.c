/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct AlarmManager {
  uint64_t armed;
  uint64_t triggered;
  uint64_t silenced;
  uint64_t total;
} AlarmManager;

/* Forward declarations */
AlarmManager am_new();
void am_arm(AlarmManager* a __attribute__((unused)));
void am_trigger(AlarmManager* a __attribute__((unused)));
void am_silence(AlarmManager* a __attribute__((unused)));
uint64_t arm_3();
uint64_t trigger_one();
uint64_t silence_count();
uint64_t total_preserved();
int main();

AlarmManager am_new() {
  return (AlarmManager){ .armed = 0ULL, .triggered = 0ULL, .silenced = 0ULL, .total = 0ULL };
}

void am_arm(AlarmManager* a __attribute__((unused))) {
  (*a).armed = ((*a).armed + 1ULL);
  (*a).total = ((*a).total + 1ULL);
}

void am_trigger(AlarmManager* a __attribute__((unused))) {
  (*a).armed = ((*a).armed - 1ULL);
  (*a).triggered = ((*a).triggered + 1ULL);
}

void am_silence(AlarmManager* a __attribute__((unused))) {
  (*a).triggered = ((*a).triggered - 1ULL);
  (*a).silenced = ((*a).silenced + 1ULL);
}

uint64_t arm_3() {
  AlarmManager a __attribute__((unused)) = am_new();
  am_arm((&a));
  am_arm((&a));
  am_arm((&a));
  return a.armed;
}

uint64_t trigger_one() {
  AlarmManager a __attribute__((unused)) = am_new();
  am_arm((&a));
  am_arm((&a));
  am_trigger((&a));
  return a.triggered;
}

uint64_t silence_count() {
  AlarmManager a __attribute__((unused)) = am_new();
  am_arm((&a));
  am_trigger((&a));
  am_silence((&a));
  return a.silenced;
}

uint64_t total_preserved() {
  AlarmManager a __attribute__((unused)) = am_new();
  am_arm((&a));
  am_arm((&a));
  am_arm((&a));
  am_trigger((&a));
  am_silence((&a));
  return a.total;
}

int main() {
  return (int)(0ULL);

}

