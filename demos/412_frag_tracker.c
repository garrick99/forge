/* Generated by FORGE compiler — do not edit.
   All proof obligations discharged. This code is correct by construction. */

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#ifndef __GNUC__
#  define __attribute__(x)
#endif

typedef struct FragTracker {
  uint64_t alloc_blocks;
  uint64_t free_fragments;
  uint64_t splits;
  uint64_t merges;
} FragTracker;

/* Forward declarations */
FragTracker ft_new(uint64_t free_fragments __attribute__((unused)));
void ft_alloc(FragTracker* f __attribute__((unused)));
void ft_free(FragTracker* f __attribute__((unused)));
void ft_split(FragTracker* f __attribute__((unused)));
void ft_merge(FragTracker* f __attribute__((unused)));
uint64_t alloc_three_free();
uint64_t two_splits();
uint64_t split_merge_net();
uint64_t total_blocks(uint64_t init __attribute__((unused)));
int main();

FragTracker ft_new(uint64_t free_fragments __attribute__((unused))) {
  return (FragTracker){ .alloc_blocks = 0, .free_fragments = free_fragments, .splits = 0, .merges = 0 };
}

void ft_alloc(FragTracker* f __attribute__((unused))) {
  (*f).alloc_blocks = ((*f).alloc_blocks + 1);
  (*f).free_fragments = ((*f).free_fragments - 1);
}

void ft_free(FragTracker* f __attribute__((unused))) {
  (*f).free_fragments = ((*f).free_fragments + 1);
  (*f).alloc_blocks = ((*f).alloc_blocks - 1);
}

void ft_split(FragTracker* f __attribute__((unused))) {
  (*f).free_fragments = ((*f).free_fragments + 1);
  (*f).splits = ((*f).splits + 1);
}

void ft_merge(FragTracker* f __attribute__((unused))) {
  (*f).free_fragments = ((*f).free_fragments - 1);
  (*f).merges = ((*f).merges + 1);
}

uint64_t alloc_three_free() {
  FragTracker f __attribute__((unused)) = ft_new(5);
  ft_alloc((&f));
  ft_alloc((&f));
  ft_alloc((&f));
  return f.free_fragments;
}

uint64_t two_splits() {
  FragTracker f __attribute__((unused)) = ft_new(5);
  ft_split((&f));
  ft_split((&f));
  return f.free_fragments;
}

uint64_t split_merge_net() {
  FragTracker f __attribute__((unused)) = ft_new(5);
  ft_split((&f));
  ft_merge((&f));
  return f.free_fragments;
}

uint64_t total_blocks(uint64_t init __attribute__((unused))) {
  FragTracker f __attribute__((unused)) = ft_new(init);
  ft_alloc((&f));
  ft_split((&f));
  ft_split((&f));
  ft_merge((&f));
  return (f.alloc_blocks + f.free_fragments);
}

int main() {
  return (int)(0);

}

